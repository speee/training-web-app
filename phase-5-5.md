# Phase 5.5: Finder / Query をTDDで共通化する

[← 前へ: Phase 5.4](./phase-5-4.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.6 →](./phase-5-6.md)

## 概要

実行前に[共通セットアップと各Phaseの確認手順](./SETUP.md#4-各phaseの実行と引き継ぎ)を確認してください。演習コードは`work/`で前のPhaseから引き継ぎます。

このフェーズでは、Phase 5.4で実装した「レコードのオブジェクト化」を土台に、**Finder / Query 処理を共通化**していきます。

これまでの実装では、おそらく次のような状態になっているはずです。

- `User.all` にSQLが書かれている
- `User.find` にSQLが書かれている
- オブジェクト変換処理が複数箇所にある
- `User` 以外のModelを作ると同じコードが増える

この状態は「動く」ものの、

- 重複が多い
- 拡張しづらい
- 他のモデルに展開しにくい

という問題があります。

このフェーズでは、それを解決するために、

> Model間で共通化できる取得処理（Finder / Query）を整理する

ことを目的とします。

---

## このフェーズの目的

以下を説明できる状態になること：

- なぜFinderを共通化したくなるのか
- なぜ `User.all` の中身を他のModelでも使いたくなるのか
- ORMにおける「Query」という概念の入り口
- 共通化と過剰抽象化の違い

---

## 到達目標

- `all`, `find`, `where` などの取得処理を共通化できる
- Modelごとの差分（テーブル名など）だけを持たせられる
- `User` 以外のモデルでも同じ仕組みが使える
- TDDで共通APIを設計できる

---

## 前提

- Phase 5.4（オブジェクト化）完了
- `User.all`, `User.find` がオブジェクトを返す状態

---

## 重要ルール

### 1. 必ずテストから書く（RED）
### 2. 最小実装で通す（GREEN）
### 3. 必要になってから共通化する（REFACTOR）

---

## このフェーズで目指す最終形（イメージ）

```ruby
User.all
User.find(1)
User.where(name: "Alice")
````

※ ただし最初からここを作らないこと
※ 必ず小さなステップで進めること

---

# 演習課題

---

## Step 1: 同じ処理が他のModelでも必要になるテストを書く（RED）

まず、「User以外でも同じことがしたい」という要求を固定します。

### テストの方向性

* `Post` モデルを作る
* `Post.all` を使いたい

### 問い

* `User` と `Post` の違いは何か？
* 何が共通で、何が違うのか？

👉 この時点では `Post` は未実装

👉 RED

---

## Step 2: Postモデルを最小実装で動かす（GREEN）

### やること

* `Post` クラスを作る
* `Post.all` を動かす

### 観察

* `User` と同じコードを書いていないか？
* コピペしていないか？

👉 この時点で重複が見えるはず

---

## Step 3: 重複を明確にする

### 観察ポイント

* DB接続処理
* SQL実行処理
* オブジェクト変換処理
* 配列化処理

### 気づき

> 「ほとんど同じコードを書いている」

---

## Step 4: 共通化したいというテストを書く（RED）

### テストの方向性

* `User` と `Post` が同じ仕組みで動く
* 共通のロジックを持ちたい

### 問い

* どこまで共通化すべきか？
* 何をModel固有として残すべきか？

👉 RED

---

## Step 5: 最小の共通化を実装する（GREEN）

### やること

* 共通処理を1箇所にまとめる
* `User` / `Post` はそれを使う

### ヒント

* 継承を使う方法がある
* 共通のベースクラスという発想

### 注意

* まだ完璧な設計にしなくてよい
* とにかく「重複が減る」ことを優先

---

## Step 6: テーブル名の違いに対応する（RED）

共通化すると、次の問題が出ます：

* `users` と `posts` は違う

### テストの方向性

* Modelごとに違うテーブルを参照する

### 問い

* テーブル名はどこに持たせるか？
* 自動化するか、明示するか？

👉 RED

---

## Step 7: テーブル名の扱いを実装する（GREEN）

### やること

* Modelごとにテーブル名を扱えるようにする

### ヒント

* クラス名とテーブル名の関係を考える

---

## Step 8: where を使いたいというテストを書く（RED）

ここで初めて「条件付き取得」を汎用化します。

### テストの方向性

```ruby
User.where(name: "Alice")
```

### 問い

* 引数はどう設計するか？
* 複数条件はどうするか？

👉 RED

---

## Step 9: where を最小実装する（GREEN）

### やること

* 条件付きSQLを生成する
* オブジェクト配列を返す

### 必須: 動的なSQLの安全性

[Phase 5.1の必須要件](./phase-5-1.md#必須要件-sqlインジェクションを防ぐ)を維持します。`where`の値は必ずバインドし、条件のキーから作るカラム名はModel側の固定した許可リストで検証します。プレースホルダはテーブル名・カラム名・並び順などSQLの構造を置き換えるものではありません。これらを動的に選ぶ場合も、開発者が定義した候補だけを使い、不明な指定はSQLを実行する前に拒否します。

- [ ] 引用符や`' OR 1=1 --`を含む値でも、意図しない行が返らない
- [ ] 未定義のキーやSQL断片を含むキーは拒否され、DBが変更されない
- [ ] 複数Modelへ共通化した後も同じテストが通る

### 注意

* SQL生成はシンプルでよい
* 完璧なクエリビルダは不要

---

## Step 10: all / find / where の共通点を整理する

### 観察

* すべて「取得処理」である
* SQLを組み立てている
* 結果をオブジェクトに変換している

### 気づき

> 「取得の流れは同じで、違うのは条件だけ」

---

## Step 11: Finder処理を整理する（REFACTOR）

### やること

* SQL実行部分を共通化
* オブジェクト生成を共通化
* 条件の違いだけ外に出す

### 目標

* Modelは「何を取得するか」だけ書く
* 共通処理はベースに閉じ込める

---

## Step 12: APIの自然さを確認する

### 問い

* `User.all` は自然か？
* `User.where` は直感的か？
* `User.find` は一貫しているか？

---

## Step 13: 「Query」という概念に気づく

ここで重要な気づき：

* `all` は条件なしのQuery
* `where` は条件付きQuery
* `find` は1件取得のQuery

### 気づき

> 「すべてQueryのバリエーション」

※ この段階ではQueryオブジェクトはまだ作らない

---

# 発展課題

---

## 課題1: 複数条件のwhere

```ruby
User.where(name: "Alice", email: "...")
```

---

## 課題2: 空結果の扱い

* 空配列でよいか？
* nilにするか？

---

# 提出物

---

## 1. テストコード

* 複数Modelでallが使える
* findが使える
* whereが使える
* 値のバインドとカラム名の許可リストにより、SQLの構造を入力で変えられない

---

## 2. 実装コード

* 共通化されたModel構造
* Finder処理

---

## 3. 設計メモ（重要）

以下を説明：

* なぜ共通化が必要になったか
* 何を共通化したか
* 何をModel固有に残したか
* ORMの「Query」とは何か

---

# レビュー観点

* TDDが守られているか
* 重複が適切に解消されているか
* 責務分離ができているか
* 過剰な抽象化になっていないか
* APIが自然か

---

# よくある失敗

---

## ❌ 最初から完璧なORMを作る

→ 過剰設計

---

## ❌ メタプログラミングに逃げる

→ 理解が浅くなる

---

## ❌ whereを複雑にしすぎる

→ 本質ではない

---

## ❌ BaseModelを肥大化させる

→ 責務が崩れる

---

# このフェーズの本質

このフェーズで得るべき理解は：

> ORMとは「SQLを隠すもの」ではなく、
> 「データ取得のパターンを共通化する仕組み」である

---

# 次フェーズへの接続

ここまで来ると、次の違和感が出ます：

* create / save がない
* updateできない
* deleteできない
* オブジェクトの状態とDBの関係が曖昧

次のフェーズでは：

👉 **Phase 5.6: 永続化（Create / Save / Update / Delete）**

---

[← 前へ: Phase 5.4](./phase-5-4.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.6 →](./phase-5-6.md)
