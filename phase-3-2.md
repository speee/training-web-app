# Phase 3.2: サーバの並行性（スレッド対応HTTPサーバ）をTDDで安全に導入する

[← 前へ: Phase 3.1](./phase-3-1.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 3.3 →](./phase-3-3.md)

## 概要

実行前に[共通セットアップと各Phaseの確認手順](./SETUP.md#4-各phaseの実行と引き継ぎ)を確認してください。演習コードは`work/`で前のPhaseから引き継ぎます。

このフェーズでは、自作HTTPサーバに**スレッドによる並行処理**を導入します。  
ただし、単にスレッド化するのではなく、

> **TDD（テスト駆動開発）を使って、安全に並行性を導入する**

ことを目的とします。

Phase 1〜3.1で構築してきたサーバは、基本的に「1リクエストずつ順番に処理する」構造です。  
このままでは、複数の同時アクセスに弱く、実務上の要求を満たせません。

一方で、単純にスレッドを導入すると、

- race condition
- 順序の不定
- 再現しづらいバグ

といった問題が発生します。

本フェーズでは、

> **並行性を導入するとき、なぜTDDやテストが重要になるのか**

を体験的に理解します。

---

## 事前準備: Minitest / SimpleCov テスト環境を構築する

このフェーズでは、マルチスレッド対応をTDDで安全に進めます。

本題である：

- 並行性
- race condition
- スレッド安全性
- TDD

に集中できるよう、このセクションでは
**最小のテスト環境を先に構築**します。

この部分は演習ではありません。  
まずは以下をそのまま構築してください。

---

# Step 0-1: 基準Gemfileを確認する

ここでいうプロジェクトルートは、[セットアップガイド](./SETUP.md)で作成した演習ディレクトリ`work/`です。教材ルートのGemfileやRakefileは上書きしないでください。

`work/Gemfile`には、Rack・WEBrickなど前のPhaseの依存関係に加えてRake、Minitest、SimpleCovが含まれています。これらの細かなバージョンは指定しません。既存の依存関係を残しつつ、導入した版でテストの実行・アサーション・カバレッジの計測ができることを確認してください。設定方法が異なる場合は各ライブラリのドキュメントを調べて調整します。解決した依存関係は自分の`Gemfile.lock`に記録します。

---

## インストール

```bash
bundle install
```

---

# Step 0-2: Rakefile を作成する

演習ディレクトリの`Rakefile`を確認してください。セットアップ済みなら以下の内容が用意されています。以後のtest_helperと最小テストも既に存在するので、内容を確認してから演習のテストを追加してください。

## Rakefile

```ruby
require "rake/testtask"

Rake::TestTask.new do |t|
  t.libs << "test"
  t.pattern = "test/**/*_test.rb"
end

task default: :test
```

---

## 確認

以下でテストが実行できることを確認してください。

```bash
bundle exec rake test
```

または：

```bash
bundle exec rake
```

---

# Step 0-3: ディレクトリ構成を作成する

以下の構成を作成してください。

```text
.
├── Gemfile
├── Gemfile.lock
├── Rakefile
├── lib/
└── test/
    └── test_helper.rb
```

---

# Step 0-4: test_helper.rb を作成する

`test/test_helper.rb` を作成してください。

## test/test_helper.rb

```ruby
require "simplecov"

SimpleCov.start do
  add_filter "/test/"
end

require "minitest/autorun"

$LOAD_PATH.unshift(File.expand_path("../lib", __dir__))
```

---

# Step 0-5: 最小テストを作成する

以下のファイルを作成してください。

## test/sample_test.rb

```ruby
require_relative "test_helper"

class SampleTest < Minitest::Test
  def test_truth
    assert true
  end
end
```

---

# Step 0-6: テストを実行する

以下を実行してください。

```bash
bundle exec rake test
```

---

## 成功条件

以下が確認できればOKです。

- テストが成功する
- `coverage/` ディレクトリが生成される
- SimpleCov のHTMLレポートが生成される

---

# Step 0-7: カバレッジレポートを確認する

生成された以下をブラウザで開いてください。

```text
coverage/index.html
```

---

## 確認すること

- テスト対象ファイルが表示されている
- 実行された行が可視化されている

---

## この準備の意図

このフェーズの主題は、
MinitestやSimpleCovの使い方そのものではありません。

本当に学ぶべきことは：

> 壊れやすい変更（並行性の導入）を、
> テストで振る舞いを固定しながら安全に進めること

です。

そのため、まずは全員が同じテスト実行環境を持った状態にします。

---

## 補足: なぜ SimpleCov を導入するのか

この研修では、カバレッジ100%を目的とはしません。

重要なのは：

> 「自分のテストが、どこを通っているのか」を可視化すること

です。

特に並行性の問題では：

- テストを書いたつもり
- しかし実際には危険箇所を通っていない

ということが起きやすいため、
可視化は重要です。

---

## 補足: なぜ Rake を使うのか

実務では：

```bash
bundle exec ruby test/foo_test.rb
```

を毎回手で打つのではなく、

```bash
bundle exec rake test
```

のように統一されたコマンドで実行します。

これは：

- CIとの統一
- チーム開発
- 実行方法の標準化

のためです。

---

## 補足: test_helper.rb の役割

`test_helper.rb` は：

- テスト共通設定
- ライブラリ読み込み
- SimpleCov設定

などをまとめるための場所です。

Railsにおける：

```text
test/test_helper.rb
```

と同じ役割です。

---

## 準備完了

ここまで完了したら、
次の Step 1 から本題である：

> 「TDDで安全に並行性を導入する」

に進んでください。


---
# サーバの並行性（スレッド対応HTTPサーバ）をTDDで安全に導入する

## このフェーズの目的

以下を説明できる状態になること：

- なぜ逐次処理サーバでは不十分なのか
- なぜ並行処理が必要なのか
- なぜ単純なスレッド化が危険なのか
- なぜ並行性の導入でTDDが有効なのか
- なぜテストだけでは不十分なのか

---

## 到達目標

- 自作HTTPサーバをマルチスレッド対応できる
- 同時アクセスに対する期待する振る舞いをテストで定義できる
- race condition を再現・観察できる
- Mutex等で最低限の対策を実装できる
- TDDの有効性と限界を説明できる

---

## 前提

- Phase 1（HTTPサーバ）
- Phase 2（WEBrick読解）
- Phase 3.1（Rack対応）
- Minitest を使えること

---

## 重要ルール

このフェーズでは、以下を必ず守ってください。

### 1. まず問題を体験する
### 2. 並行性を導入する前に、期待する振る舞いをテストで固定する
### 3. RED → GREEN → REFACTOR を意識する
### 4. テストが通っても安心しすぎない

---

# 演習課題

---

## Step 1: 逐次処理サーバの限界を体験する

まずは、現在のサーバがなぜ不十分かを体験します。

### 課題

自作サーバのリクエスト処理に、意図的に遅延を入れてください。

### 実験

- ブラウザで複数タブから同時アクセスする
- または複数回連続でアクセスする

### 観察

- リクエストは同時に処理されるか？
- 1つの遅いリクエストが他をブロックしていないか？

### 気づき

> 現在のサーバは「1つずつしか処理できない」

---

## Step 2: 期待する振る舞いを先にテストで固定する（RED）

ここでいきなりスレッド化せず、  
**「何ができるようになってほしいか」** を先にテストで定義します。

### テストの方向性

- 複数のリクエストが同時に処理開始できること
- あるリクエストの遅延が他のリクエストを完全には止めないこと

### ヒント

- 実サーバを起動してHTTPリクエストを送る統合テストでもよい
- まずは「逐次処理では失敗する」ことが確認できればよい

### 状態

👉 この時点では失敗してよい  
👉 これが RED

---

## Step 3: 最小のスレッド化を行う（GREEN）

### やること

- 接続ごとにスレッドを作る
- 各リクエストを独立に処理する

### 注意

- まずは「同時に動く」ことだけを目標にする
- まだ安全性は十分でなくてよい

### 確認

- Step 2 のテストが通るか確認する

---

## Step 4: スレッド化で壊れるケースをテストで観察する（RED）

ここからが本フェーズの核心です。

スレッド化すると、今度は**安全性**の問題が出ます。  
それをまずテストで固定します。

### 課題

共有状態を持つ小さなクラスを用意してください。  
例：

- リクエスト数カウンタ
- 簡単なメモリ上キャッシュ
- 接続数カウンタ

### テストの方向性

- シングルスレッドでは通る
- マルチスレッドでは不安定になる

### 例として観察したいこと

- カウントが期待値に届かない
- 成功したり失敗したりする
- 順序が保証されない

### 気づき

> スレッドの問題は「動く / 動かない」ではなく「不安定」である

---

## Step 5: race condition の原因を理解する

### 課題

以下を説明してください：

- なぜ期待した値にならないのか
- どこで競合が起きているのか
- なぜシングルスレッドでは問題が見えにくかったのか

### キーワード

- race condition
- 共有状態
- 読み取りと書き込みの間の割り込み

---

## Step 6: 最低限の対策をテストから導入する（GREEN）

ここで初めて対策を入れます。

### テストの方向性

- マルチスレッド時でも値が安定する
- 共有状態が壊れない

### やること

- Mutex などを使って保護する
- 必要最小限の範囲に適用する

### 注意

- 全体を大きくロックしすぎない
- どこにロックが必要かを説明できること

---

## Step 7: HTTPサーバ本体に応用する

ここまでの小さな共有状態の問題を、  
自作HTTPサーバに引き戻して考えます。

### 課題

自作サーバの中で、共有状態を持つ場所がないか確認してください。

例：

- ログ
- 接続数
- グローバル変数
- クラス変数
- キャッシュ的なもの

### 問い

- どこが危険か？
- どこは安全か？
- Rackアプリ側に共有状態があると何が起きるか？

---

## Step 8: TDDの有効性と限界を振り返る

このフェーズでは、TDDを導入しました。  
ただし、並行性の世界ではテストは万能ではありません。

### 課題

以下を説明してください：

- なぜTDDは有効だったのか
- どのような振る舞いを先に固定できたのか
- なぜそれでもバグを100%防げないのか
- 並行性では、設計がなぜ重要なのか

### 到達してほしい理解

> TDDは「安全に進めるための重要な道具」だが、  
> 並行性においてはそれだけでは不十分である

---

## Step 9: WEBrick / Rack / 実務構成と比較する

### 課題

以下を調べて比較してください：

- WEBrickはどのように並行処理しているか
- Rackアプリはスレッドセーフである必要があるか
- 実務でPumaなどがなぜ慎重に設計されているのか

### 問い

- なぜ本物のWebサーバはもっと複雑なのか？
- どの複雑さが「必要な複雑さ」なのか？

---

# 発展課題

---

## 課題1: テストの再現率を上げる

- スレッド数を増やす
- 実行回数を増やす
- 意図的な遅延を入れる

### 問い

- なぜ再現率が変わるのか？

---

## 課題2: before/after で性能を比較する

- 逐次処理サーバ
- スレッド対応サーバ

### 問い

- 何が改善されたか？
- 何が新しく難しくなったか？

---

## 課題3: Rackアプリに共有状態を持たせる

- 共有カウンタ
- メモリ上ストア

### 問い

- Rack対応後でも同じ問題は起きるか？

---

# 提出物

---

## 1. テストコード

最低限、以下を含むこと：

- 逐次処理サーバの限界を示すテスト
- スレッド化後の期待する振る舞いを示すテスト
- race condition を観察するテスト
- 対策後に安定することを示すテスト

---

## 2. 実装コード

- スレッド対応HTTPサーバ
- 共有状態を扱う小さなサンプルコード
- 必要に応じた Mutex 等の対策

---

## 3. レポート

以下を説明してください：

- なぜ並行処理が必要か
- なぜ単純なスレッド化が危険か
- なぜこのフェーズでTDDを導入したのか
- TDDの有効性と限界
- 自作サーバにおける危険箇所と対策

---

# レビュー観点

- 逐次処理の限界を体験できているか
- 期待する振る舞いを先にテストで定義できているか
- race condition を観察・説明できているか
- Mutex の適用が妥当か
- TDDの意義と限界を理解しているか

---

# よくある失敗

---

## ❌ 先にスレッド化してからテストを書く

→ このフェーズの主題は「安全に進めること」

---

## ❌ テストが「たまたま通る」ことを成功だと思う

→ 不安定さそのものが学び

---

## ❌ Mutexを広くかけすぎる

→ 動くが、本質的な理解が浅くなる

---

## ❌ 「TDDなら安全」と思い込む

→ 並行性ではそれでも漏れる

---

# このフェーズの本質

このフェーズで得るべき理解は：

> 並行性の導入では、  
> 「まず動かす」ではなく「何を壊してはいけないかを先に定義する」ことが重要である

そして同時に、

> TDDは安全に進めるための強い道具だが、  
> 並行性では設計・共有状態の扱い・責務分離がさらに重要になる

---

# Before / After

## Before

- 単一リクエストの処理ができる
- 逐次処理で動いている

## After

- 同時アクセスを前提に設計を考えられる
- スレッド安全性を意識できる
- TDDを「安全に変更する道具」として使える

---

# 最後に

このフェーズを理解すると、

- 本番環境でなぜ並行性が難しいのか
- なぜWebサーバやフレームワークが複雑なのか
- なぜ実務で「安全に変更する」ためのテストが重要なのか

が腹落ちします。

ここは、単にスレッドを学ぶフェーズではありません。

> **「安全に進化させる設計と開発プロセス」を学ぶフェーズ**です。

---

[← 前へ: Phase 3.1](./phase-3-1.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 3.3 →](./phase-3-3.md)
