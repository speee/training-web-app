# Phase 5.2: 1テーブル専用ModelをTDDで導入する

[← 前へ: Phase 5.1](./phase-5-1.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.3 →](./phase-5-3.md)

## 概要

実行前に[共通セットアップと各Phaseの確認手順](./SETUP.md#4-各phaseの実行と引き継ぎ)を確認してください。演習コードは`work/`で前のPhaseから引き継ぎます。

このフェーズでは、Phase 5.1で体験した「生SQLのつらさ」を解消するために、**1テーブル専用のModelクラス** を TDD（テスト駆動開発）で導入します。

Phase 5.1では、Controllerの中で直接SQLを書きました。

その結果、次のような問題が見えたはずです。

- ControllerにSQLが書かれている
- 同じようなSELECTが何度も出てくる
- DBの戻り値（配列やハッシュ）が扱いにくい
- DBアクセスの責務がControllerに漏れている

このフェーズでは、それを解決するために、

> 「usersテーブルへのアクセスを1箇所にまとめる」

という最小限の抽象化として、Modelを導入します。

---

## このフェーズの目的

以下を説明できる状態になること：

- なぜModelが必要なのか
- ControllerとModelの責務の違い
- なぜ「1テーブル専用」から始めるのか
- SQLを隠すとはどういうことか（完全に隠すわけではない）

---

## 到達目標

- `User` クラスを導入できる
- `User.all` で全件取得できる
- `User.find(id)` で1件取得できる
- ControllerからSQLが消える
- DBアクセスがModelに集約される
- TDDでModelのAPIを設計できる

---

## 前提

- [Phase 5.1](./phase-5-1.md)（生SQLのつらさ）完了
- SQLite3でDB操作ができる
- ControllerからDBアクセスできる状態

---

## 重要ルール

### 1. 必ずテストから書く（RED）
### 2. 最小実装で通す（GREEN）
### 3. 抽象化は必要になってから（REFACTOR）

---

## このフェーズで目指す最終形（イメージ）

```ruby
users = User.all
user = User.find(1)
````

Controllerでは：

```ruby
@users = User.all
@user = User.find(params["id"])
```

※ ただし最初からこれを作ってはいけません
※ 必ずテストから段階的に進めてください

---

# 演習課題

---

## Step 1: ControllerからSQLを消したいというテストを書く（RED）

まず「Modelが欲しい」という要求をテストで固定します。

### テストの方向性

* Controllerから直接SQLを書かない
* 代わりにModel経由で取得したい

### 例（考えるべきこと）

* `User.all` を呼びたい
* それが配列として返ってきてほしい

👉 この時点ではまだ `User` クラスは存在しない

👉 RED

---

## Step 2: 最小のUserクラスを作る（GREEN）

### やること

* `User` クラスを作る
* `self.all` メソッドを追加する
* 内部でSQLを実行する

### ヒント

* まずは Controller に書いていたSQLをそのまま移す
* DB接続をどう取得するかは設計ポイント

### 問い

* DB接続はModelが持つべきか？
* Controllerから渡すべきか？

---

## Step 3: ControllerをModel経由に書き換える（REFACTOR）

### やること

* Controller内のSQLを削除する
* `User.all` を使う

### 観察

* Controllerがシンプルになったか？
* 何の責務がどこに移動したか？

---

## Step 4: find(id) を使いたいというテストを書く（RED）

次に、1件取得の要件を固定します。

### テストの方向性

* `User.find(1)` のように書きたい
* 特定のユーザーを取得したい

### 問い

* 見つからない場合どうするか？

（例）

* nilを返す
* 例外を投げる

👉 どちらにするかをテストで決める

---

## Step 5: find を最小実装する（GREEN）

### やること

* `WHERE id = ?` のSQLを書く
* 1件だけ取得する

### 観察

* `.first` のような処理が必要か？
* allとの共通点は何か？

---

## Step 6: all と find の重複に気づく

ここでコードを見直してください。

### 観察ポイント

* SQL実行処理が重複していないか？
* DB接続処理が重複していないか？
* 結果の扱いがバラバラではないか？

### 気づき

> 「共通処理をまとめたくなる」

---

## Step 7: 最小限の共通化を行う（REFACTOR）

### やること

* SQL実行処理を1箇所にまとめる
* DB接続の取得方法を整理する

### 注意

まだ BaseModel は作らないこと
（次フェーズでやる）

---

## Step 8: Controllerから見た使いやすさをテストで確認する（RED）

### テストの方向性

* ControllerはSQLを知らない
* `User.all` / `User.find` だけ使う

### 問い

* ControllerはDBのことをどこまで知っているべきか？

---

## Step 9: 最小修正で通す（GREEN）

---

## Step 10: Modelの責務を言語化する

このフェーズの最重要ポイントです。

以下を説明してください：

* Modelは何をするクラスか？
* Controllerと何が違うのか？
* SQLはどこにあるべきか？

---

# 発展課題

---

## 課題1: findで見つからなかった場合の別設計

* `find_by_id`
* `find!`

などの設計を考える

---

## 課題2: DBの戻り値の形に違和感を持つ

### 問い

* 今の `User.all` の戻り値は何か？
* それは本当に「User」と言えるか？

👉 次フェーズへの伏線

---

## 課題3: Modelが肥大化する兆候を観察する

* SQLが増えていないか？
* メソッドが増えていないか？

---

# 提出物

---

## 1. テストコード

* `User.all`
* `User.find`
* Controller経由での利用

---

## 2. 実装コード

* Userクラス
* Controller修正

---

## 3. 設計メモ（重要）

以下を説明：

* なぜModelが必要になったか
* Controllerから何が消えたか
* Modelに何が集まったか
* 今のModelの限界は何か

---

# レビュー観点

* TDDが守られているか
* Modelの責務が明確か
* Controllerがシンプルになっているか
* 不要な抽象化をしていないか
* 「なぜ必要か」を説明できるか

---

# よくある失敗

---

## ❌ 最初からORMっぽく作る

→ まだ早い

---

## ❌ BaseModelを先に作る

→ 重複が見えてから

---

## ❌ Controllerにロジックを残す

→ Modelへ寄せる

---

## ❌ 戻り値の違和感を無視する

→ 次フェーズの重要な気づき

---

# このフェーズの本質

このフェーズで得るべき理解は：

> Modelは「SQLを隠すためのもの」ではなく、
> 「データアクセスの責務をControllerから分離するためのもの」

---

# 次フェーズへの接続

ここまで来ると、次の課題が出ます：

* Modelの挙動を確認するたびにControllerやブラウザを経由している
* `User.all` や `User.find` だけを素早く試したい
* ORMを育てるための検証環境がない

次のフェーズでは、これを解決するためにConsoleモードを導入します：

👉 **Phase 5.3: Consoleモードを実装する — 自作ORMを対話的に検証する**

---

[← 前へ: Phase 5.1](./phase-5-1.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.3 →](./phase-5-3.md)
