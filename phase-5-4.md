# Phase 5.4: レコードをオブジェクトとして扱う（TDD）

[← 前へ: Phase 5.3](./phase-5-3.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.5 →](./phase-5-5.md)

## 概要

実行前に[共通セットアップと各Phaseの確認手順](./SETUP.md#4-各phaseの実行と引き継ぎ)を確認してください。演習コードは`work/`で前のPhaseから引き継ぎます。

このフェーズでは、Phase 5.2で導入し、Phase 5.3のConsoleで対話的に観察した Model をさらに発展させ、**DBの1行を「Rubyオブジェクト」として扱えるようにする**ことを、TDDで実装します。

Phase 5.2の時点では、`User.all` や `User.find` は実装できているはずです。

しかし、その戻り値はおそらく次のような形になっているはずです：

- 配列
- 配列の配列
- ハッシュ
- DBライブラリ依存の構造

この状態では、次のような違和感があるはずです。

- `user["name"]` のようなアクセスが気持ち悪い
- 「User」と言っているのにただのデータ構造
- Viewでの扱いが直感的でない
- アプリケーションコードがDBの都合を知りすぎている

このフェーズでは、それを解決するために、

> 「DBの1行をオブジェクトに変換する」

という仕組みを導入します。

---

## このフェーズの目的

以下を説明できる状態になること：

- なぜレコードをオブジェクトにしたくなるのか
- ORMの本質は何か（SQL隠蔽ではなくオブジェクト化）
- 「データ」と「オブジェクト」の違い
- Modelが単なるDAOではない理由

---

## 到達目標

- `User.all` が `User` オブジェクトの配列を返す
- `User.find` が `User` オブジェクトを返す
- `user.name` のようにアクセスできる
- Controller / ViewからDBの内部構造が見えなくなる
- TDDでオブジェクト設計を進められる

---

## 前提

- Phase 5.3（Consoleモード）完了
- `User.all`, `User.find` が動作している
- ConsoleでModelの戻り値を観察できる

---

## 重要ルール

### 1. テストから書く（RED）
### 2. 最小実装で通す（GREEN）
### 3. 抽象化は必要になってから（REFACTOR）

---

## このフェーズで目指す最終形（イメージ）

```ruby
users = User.all
user = users.first

user.name   # => "Alice"
user.email  # => "alice@example.com"
````

---

# 演習課題

---

## Step 1: オブジェクトとして扱いたいというテストを書く（RED）

まず、現在の違和感をテストで固定します。

### テストの方向性

* `User.all` の戻り値の中身がオブジェクトである
* 属性にメソッドでアクセスできる

### 例（考えるべきこと）

* `users.first.name` のように書きたい
* `["name"]` のようなアクセスをやめたい

👉 この時点では失敗するはず

👉 RED

---

## Step 2: 最小のオブジェクト変換を実装する（GREEN）

### やること

* DBから取得した1行を `User` インスタンスに変換する
* `initialize` を使って値を持たせる

### ヒント

* 1件分のデータから始めるとよい
* 配列 or ハッシュ → オブジェクト

### 問い

* DBの行データはどの形式で渡ってくるか？
* それをどうインスタンス変数にするか？

---

## Step 3: `all` をオブジェクト配列にする（REFACTOR）

### やること

* `User.all` の戻り値を変換する
* 各行を `User` インスタンスにする

### 観察

* map のような処理が必要になるか？
* DB結果とオブジェクトの対応関係はどうなっているか？

---

## Step 4: `find` もオブジェクトを返すようにする（RED → GREEN）

### テストの方向性

* `User.find(1)` がオブジェクトを返す
* `user.name` が使える

### 観察

* `all` と `find` の共通処理は何か？

---

## Step 5: 属性アクセスの違和感を解消する（RED）

現在の実装では、おそらく以下のような状態です：

* `@name` を直接参照している
* `attr_reader` がない
* アクセス方法が不自然

### テストの方向性

* `user.name` が使える
* `user.email` が使える

👉 RED

---

## Step 6: 属性アクセサを導入する（GREEN）

### やること

* 属性に対するメソッドを定義する

### ヒント

* 明示的に定義する方法
* Rubyの仕組みを使う方法

### 問い

* カラムが増えたらどうするか？

---

## Step 7: Viewからの扱いやすさを確認する（RED）

### テストの方向性

* Viewで `user.name` が使える
* DB構造を意識せずに表示できる

👉 RED

---

## Step 8: 最小修正で通す（GREEN）

---

## Step 9: DBの構造が漏れていないか確認する

### 観察ポイント

* ControllerにDBの構造が残っていないか
* ViewにDBの形式が漏れていないか

### 気づき

> 「オブジェクトにすることでDBの詳細が隠れる」

---

## Step 10: 重複に気づく

ここでコードを見直してください。

### 観察

* 初期化処理が繰り返されていないか
* カラム名と属性の対応が手書きになっていないか

### 気づき

> 「もっと自動化したくなる」

※ まだやらない（次フェーズ）

---

## Step 11: 最小限の整理を行う（REFACTOR）

### やること

* 初期化処理をまとめる
* オブジェクト生成の責務を整理する

### 注意

* まだBaseModelは作らない
* まだメタプログラミングしない

---

## Step 12: 「なぜオブジェクトにしたのか」を言語化する

### 必ず説明すること

* なぜ配列やハッシュではダメだったのか
* オブジェクトにすることで何が良くなったか
* ORMの本質は何か

---

# 発展課題

---

## 課題1: `user["name"]` を完全に禁止する

### 問い

* APIとしてどちらを許すべきか？
* なぜ？

---

## 課題2: 属性が増えた場合の課題を観察する

* カラムが増えたらどうなるか？
* 毎回メソッドを書くのか？

👉 次フェーズの伏線

---

## 課題3: DBの型とRubyの型の違いを考える

### 問い

* INTEGERはどう扱われるか？
* NULLはどうなるか？

---

# 提出物

---

## 1. テストコード

* `User.all` がオブジェクトを返す
* `User.find` がオブジェクトを返す
* 属性にメソッドでアクセスできる

---

## 2. 実装コード

* Userクラス
* 初期化処理
* 属性アクセス

---

## 3. 設計メモ（重要）

以下を説明：

* なぜレコードをオブジェクトにしたのか
* 配列 / ハッシュとの違い
* Modelの責務はどう変わったか
* 次にどんな抽象化が欲しくなるか

---

# レビュー観点

* TDDが守られているか
* オブジェクト化の意図が理解できているか
* DBの詳細が隠蔽されているか
* 過剰な抽象化をしていないか
* 次フェーズへの問題意識があるか

---

# よくある失敗

---

## ❌ ただのStructで満足する

→ 本質は「変換」ではなく「責務分離」

---

## ❌ いきなりメタプログラミングする

→ まだ早い

---

## ❌ DBの構造をそのまま露出する

→ ORMの意味がない

---

## ❌ 配列アクセスを残す

→ 一貫性が崩れる

---

# このフェーズの本質

このフェーズで得るべき理解は：

> ORMの本質は、SQLを隠すことではなく、
> 永続化されたデータを「オブジェクト」として扱えるようにすること

---

# 次フェーズへの接続

ここまで来ると、次の違和感が出ます：

* カラムごとにメソッドを書くのが面倒
* Modelごとに同じコードがある
* `User` 以外のモデルも作りたい

次のフェーズでは：

👉 **Phase 5.5: Finder / Query の共通化**

ORMの「仕組み」に入ります

---

[← 前へ: Phase 5.3](./phase-5-3.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 5.5 →](./phase-5-5.md)
