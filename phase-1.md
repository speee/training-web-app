# Phase 1: 低レイヤー理解（HTTPサーバ実装）演習

[← 前へ: セットアップ](./SETUP.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 2 →](./phase-2.md)

## 概要

実行前に[共通セットアップと各Phaseの確認手順](./SETUP.md#4-各phaseの実行と引き継ぎ)を確認してください。演習コードは`work/`で前のPhaseから引き継ぎます。

このフェーズでは、Webシステムの最も基礎となる「HTTPサーバ」を  
**RubyのSocketライブラリのみを使って自分の手で実装**します。

実装課題ではフレームワークやHTTPサーバライブラリを使いません。Step 0の観察だけは既存のWEBrickを利用します。

対象は**TCP上のHTTP/1.1の一部**です。開始行とヘッダはテキスト形式ですが、ボディは画像など任意のバイト列も扱います。HTTP全般を「ただのテキスト通信」と捉えないでください。HTTP/2・HTTP/3との違いは後述します。

---

## このフェーズの目的

以下を「理解して説明できる状態」になること：

- HTTPリクエストとは何か
- HTTPレスポンスとは何か
- クライアントとサーバの通信の流れ
- なぜブラウザにHTMLが表示されるのか

---

## 到達目標

以下の仕様を満たすHTTPサーバを実装すること：

- loopbackでTCP接続を受け付ける
- HTTP/1.1リクエストのヘッダ末尾まで受信する
- ボディなしのGETリクエストに対応する
- CRLF・Content-Length・Connection: closeを使い、固定のHTMLを返して接続を閉じる
- ブラウザからアクセスして表示できる

---

## 前提条件

- Rubyがインストールされていること
- ターミナル操作ができること
- 基本的なRuby文法を理解していること

---

## 演習課題

## Step 0: ローカルのWebサーバと対話する（事前体験）

実装前に、自分で起動したWEBrickへHTTP/1.1リクエストを送り、ブラウザが行う通信の一部を観察します。外部の第三者サイトへの接続は不要です。ここでは平文のHTTPを使い、HTTPSは[Phase 3.3](./phase-3-3.md)で扱います。

この事前体験の目的は、リクエストを自分で組み立て、返ってきたレスポンスを読むことで、次の点を体感的に理解することです。

- HTTP/1.1では、クライアントが何を要求し、サーバがどのような形式で応答するのか
- ブラウザがページを表示するまでに行う、リクエストの送信とレスポンスの受信とは何か
- ステータス・ヘッダ・ボディから、サーバの処理結果や返された内容をどう読み取るのか
- CRLF・Content-Length・接続の終了が、メッセージの区切りを判断する上でどのような役割を持つのか

この理解を、Step 1以降で「自分がサーバ側として何を受け取り、何を返す必要があるか」を考える土台にします。

### Step 0-1: 観察用サーバを起動する

[セットアップガイド](./SETUP.md)で準備した`work/`で実行します。公開対象を観察用ディレクトリだけに限定します。

```bash
mkdir -p http-observation
printf '%s' '<h1>Hello HTTP</h1>' > http-observation/index.html
bundle exec ruby -run -e httpd -- --bind-address=127.0.0.1 --port=3000 http-observation
```

この端末を起動したままにします。ポート3000が使用中なら既存の演習サーバを停止してください。接続先は自分のPCの`127.0.0.1:3000`です。

### Step 0-2: CRLFを指定してリクエストを送る

別の端末を開き、同じ`work/`で実行します。これは観察用のTCPクライアントであり、HTTPサーバの解答ではありません。

```bash
bundle exec ruby -rsocket -e 'TCPSocket.open("127.0.0.1", 3000) { |socket| socket.write("GET / HTTP/1.1\r\nHost: localhost:3000\r\nConnection: close\r\n\r\n"); print socket.read }'
```

送信内容を見やすく書くと、次のようになります。以後のHTTPの表示例では改行を見やすく表示していますが、**開始行・各ヘッダ行の末尾はCRLF（`\r\n`）**です。最後のヘッダ行の後にさらにCRLFを送り、`\r\n\r\n`でヘッダを終えます。

```http
GET / HTTP/1.1
Host: localhost:3000
Connection: close

```

HTTP/1.1では`Host`が必要です。上のGETはボディを送らないため、リクエストにContent-Lengthは不要です。`Connection: close`は、この応答後に接続を閉じることを要求しています。

手入力を試す場合は`telnet 127.0.0.1 3000`などでも接続できます。ただしEnterが送る改行はクライアントの設定によるため、正確なバイト列を送る上のコマンドを基準にしてください。telnetの追加インストールは必須ではありません。

### Step 0-3: レスポンスを観察する

次の要素を探してください。WEBrickが付けるヘッダの種類や順序は版によって変わることがあります。

```http
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 19
Connection: close

<h1>Hello HTTP</h1>
```

- ステータスライン: HTTPバージョンと処理結果
- ヘッダ: 本文の種類、バイト数、接続を閉じる指定など
- 空行: ヘッダの終端。本文全体の終端ではない
- ボディ: 今回は末尾に改行のない19バイトのHTML

### Step 0-4: ローカルで実験する

Step 0-2のコマンドのリクエスト文字列を変更し、その都度接続し直します。

1. `Host: localhost:3000\r\n`を削除して応答を観察する。[HTTP/1.1の仕様](https://www.rfc-editor.org/rfc/rfc9112.html#section-3.2)では`400 Bad Request`が必要だが、観察用WEBrickが受理する場合もある。実装が受理することと仕様に適合することを区別する。
2. `GET /`を`GET /missing`に変える。作成していないファイルなので、`404 Not Found`になることを確認する。
3. `HTTP/1.1`を`HTTP/1.0`に変え、Hostも削除する。応答と接続の違いを観察する（HTTP/1.0の実装は本課題の対象外）。

観察が終わったら、サーバ側の端末で`Ctrl-C`を押して停止し、Step 1でポート3000を使えるようにします。

### HTTP/1.1のメッセージの区切り（フレーミング）

TCPはバイト列を運びます。1回の受信がリクエスト1個と一致する保証はなく、途中で分割されたり、複数行がまとめて届いたりします。受信側で境界を判断する必要があります。

- CRLFと空行で開始行・ヘッダの境界を表す。改行はLF（`\n`）だけで生成しない。
- 本演習のGETへの200/404応答は、`Content-Length`でボディの**バイト数**を示す。Rubyでは文字数の`length`ではなく`bytesize`を使う。ヘッダのバイト数は含めず、本文に改行を付ければその分も数える。
- HTTP/1.1は原則として接続を再利用する。本演習は`Connection: close`を明示し、応答を最後まで書いた後にソケットを閉じる。ヘッダを書くだけでは接続は閉じない。
- 仕様では`Transfer-Encoding: chunked`で本文を区切る方法や、条件によって接続終了を本文末尾とするレスポンスもある。後者は正常終了と通信断を区別しづらい。接続終了でリクエスト本文を区切ることはできない。

この演習ではボディなしGET・固定長レスポンス・1接続1リクエストに限定します。chunked、接続の再利用、HEADや204/304など本文を送らない応答、異常な入力の厳密な検証は実装対象外です。これは学習用の部分実装で、HTTP/1.1仕様全体への準拠や本番利用を保証しません。詳細は[RFC 9112 §2](https://www.rfc-editor.org/rfc/rfc9112.html#section-2)、[§6](https://www.rfc-editor.org/rfc/rfc9112.html#section-6)、[§9](https://www.rfc-editor.org/rfc/rfc9112.html#section-9)を参照してください。

### HTTP/2・HTTP/3との違い

| バージョン | この演習との違い |
| --- | --- |
| HTTP/1.1 | 開始行・ヘッダはテキスト形式。ここではTCP上で直接送受信する |
| HTTP/2 | TCP上のバイナリフレーミングでヘッダやデータを送る。telnetでHTTP/1.1の行を入力してもHTTP/2にはならない |
| HTTP/3 | QUIC上のバイナリフレーミングを使う。QUICはUDPを使い、TLS 1.3を組み込む |

メソッドやステータスといったHTTPの意味は共有しますが、通信上の表現は異なります。ブラウザの開発者ツールで読める表示が、そのまま通信上のバイト列とは限りません。参照: [RFC 9113 §4](https://www.rfc-editor.org/rfc/rfc9113.html#section-4)、[RFC 9114 §2](https://www.rfc-editor.org/rfc/rfc9114.html#section-2)。

### よくあるつまずき

- 応答がない: リクエスト末尾の`\r\n\r\n`と、`Connection: close`を確認する。
- 接続できない: 観察用サーバの起動、loopbackのアドレス、ポート番号、エラーログを確認する。
- 本文が途中で切れる: Content-Lengthのバイト数と受信した本文を比較する。途中の切断を正常と決めつけない。

ここまでで、HTTP/1.1の要求を自分で組み立て、応答を読み取れました。次はサーバ側を実装します。

### Step 1: TCPサーバを立ち上げる

以下を満たすプログラムを作成してください：

- `Socket` または `TCPServer` を使用する
    - [Socket](https://docs.ruby-lang.org/ja/latest/class/Socket.html)
- loopbackのアドレス（`127.0.0.1`）と任意のポート（例：3000）で待ち受ける
- ホスト指定を省略しない（`TCPServer.new("127.0.0.1", 3000)`のように明示する）
- クライアント接続を受け付ける

#### 確認方法

```
telnet 127.0.0.1 3000
```

接続できればOK。telnetがなければStep 0-2のTCPクライアントで接続を試してください。まだ応答を実装していないので、終了はクライアント側で`Ctrl-C`を押します。

---

### Step 2: リクエストを受け取る

クライアントから送られてくるデータを受信し、
ターミナルに出力してください。開始行だけでなく、ヘッダ末尾の空行（`\r\n`）まで読み取ります。ボディなしGETの演習なので、クライアントが接続を閉じるまで待つ`read`は使いません。

#### 確認ポイント

ブラウザで以下にアクセス：

```
http://localhost:3000/
```

以下のようなテキストが出力されること：

```
GET / HTTP/1.1
Host: localhost:3000
User-Agent: ...
```


---

### Step 3: HTTPレスポンスを返す

以下の形式でレスポンスを返してください。開始行とヘッダにはCRLFを使います。例の本文は末尾改行なしの20バイトです。本文送信後に接続を閉じます：


```
HTTP/1.1 200 OK
Content-Type: text/html
Content-Length: 20
Connection: close

<h1>Hello World</h1>
```

#### 成功条件

ブラウザにアクセスしたとき：

* 「Hello World」と表示される

### Step 4: リクエストラインを解析する

受信したリクエストから以下を取得してください：

```
HTTPメソッド（GETなど）
パス（/ や /test など）
```

#### 例

* method: GET
* path: /test

### Step 5: パスによる分岐

以下のようにレスポンスを変えてください：

| パス       | レスポンス         |
| -------- | ------------- |
| `/`      | Hello World   |
| `/about` | About Page    |
| その他      | 404 Not Found |


### Step 6: 404レスポンスの実装

存在しないパスの場合：
```
HTTP/1.1 404 Not Found
Content-Type: text/html
Content-Length: 18
Connection: close

<h1>Not Found</h1>
```

を返してください。本文は末尾改行なしの18バイトです。200応答と同じくCRLFで区切り、本文を書いた後に接続を閉じます。

# 発展課題（余裕があれば）

## 課題1: クエリパラメータ対応

```
/hello?name=Ota
```

のようなリクエストに対して：

```
Hello Ota
```

と返す


## 課題2: レスポンスヘッダを理解する

本文を日本語に変更し、`length`と`bytesize`の違いを確認する。正しいContent-Lengthで最後まで表示できることを確かめる。

Connection: closeを付ける理由と、接続を再利用する場合に必要となる処理を説明する。


## 課題3: HTMLをファイルから読み込む

.html ファイルを読み込んで返す


## 課題4: 指定されたディレクトリのファイル一覧を返す

リクエストパスで指定されたディレクトリのファイルやディレクトリの一覧をHTMLとして返す

この課題に取り組む場合、以下を完了条件とします。課題3でリクエストからファイルを選ぶ場合も同じ条件を適用します。

- [ ] 配信専用ルートを固定し、ソースコード・`.git`・秘密鍵をその外に置く
- [ ] URLのパスをデコード・正規化し、不正なエンコーディングを拒否する。検査後に再デコードして別のパスにしない
- [ ] `..`や絶対パス、シンボリックリンクの解決後も、対象が配信ルート内であることを確認する。単純な文字列の前方一致では、`public-other`を`public`配下と誤認し得るため、パス要素の境界まで検査する
- [ ] 通常の配下ディレクトリは表示でき、`../`、`%2e%2e%2f`、二重エンコード、ルート外を指すシンボリックリンク、似た名前の隣接ディレクトリでは、ルート外の内容を返さないテストを通す
- [ ] 拒否時は400・403・404など決めたエラーを返し、実パスやスタックトレースを応答に含めない
- [ ] ファイル名はHTMLエスケープして表示し、リンクのパス要素はURLエンコードする。`<`・`&`などを含むファイル名でもHTMLとして解釈させない

ローカルの一時ディレクトリとダミーファイルで検証してください。配信中のファイル差し替えによる競合など、ここまでで防げない問題も残ります。この課題を通しても外部公開してはいけません。

## 提出物

以下を提出してください：

* トレーニング用のGitHubにコミットしてください
* ソースコード
* 動作確認手順（README）
* 自分の言葉での説明（重要）


## レビュー観点

以下の観点でレビューします：

* HTTP/1.1のCRLF・本文長・接続終了の役割と、今回の実装範囲を理解しているか
* なぜその実装になるか説明できるか
* 最小構成でシンプルに書けているか
* 不要な抽象化をしていないか

## AI利用ルール（再掲）

* コード生成は禁止
* 理解・調査・解説用途はOK

## よくあるつまずきポイント

### 1.  なぜブラウザに表示されるのかわからない

→ HTTPレスポンスのフォーマットを確認すること

### 2. 改行コードの扱い

HTTP/1.1の開始行・ヘッダ行の末尾には以下を使い、ヘッダの終端には2つ続けます：

```
\r\n
```


### 3. 接続が閉じられない

レスポンス後にソケットを閉じること


## 最後に

このフェーズはシンプルですが非常に重要です。

ここを理解できると：

* フレームワークの挙動が見えるようになる
* バグの原因が追えるようになる
* AIのコードの良し悪しが判断できるようになる

必ず手を動かして理解してください。

---

[← 前へ: セットアップ](./SETUP.md) | [目次](./README.md#カリキュラムテキスト) | [次へ: Phase 2 →](./phase-2.md)
